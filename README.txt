This is the document we use to communicate when some team members aren't here.
Record what was done and what still needs to be done.
Don't delete lines from this file, just add to them below.

Monday Jan 6 2014
=================
Hey guys! You may have noticed that the repository changed how it looks in terms of directory layout a lot.
I wanted the repo to contain all the setting and build files so those could be synchronized as well.
The easiest way to fix the version on the robotics computer is probably to do a clean clone
(i.e. git clone git@github.com:team1815/FRC.git )
